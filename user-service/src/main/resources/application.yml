spring:
  datasource:
    url: jdbc:postgresql://postgres-user:5432/userdb
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: redis
      port: 6379

jwt:
  secret: "yLd/5MwNwJqsISm0jmtfJoufVvusFrCgnjbelAbTce17KbTt2qEf/BmOx7SXbnYAGH4Gg/NoDNPHoe71sI6fWg=="
  expiration: 86400000

server:
  port: 8081

logging:
  level:
    root: INFO
    com.example.user: DEBUG

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: "*"
# Alias for @Value resolution
redis:
  host: redis
  port: 6379
